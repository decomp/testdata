strict digraph "acl_errno_valid" {
	// Node definitions.
	1 [entry=true];
	3;
	2;

	// Edge definitions.
	1 -> 3 [
		cond="case (x=95)"
		label="case (x=95)"
	];
	1 -> 2 [
		cond="default case"
		label="default case"
	];
	2 -> 3;
}